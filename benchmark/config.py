"""Central paths and trusted upstream sources for benchmark acquisition."""

from pathlib import Path


PROJECT_ROOT = Path(__file__).resolve().parents[1]
BENCHMARK_ROOT = PROJECT_ROOT / "benchmark"
CODEFLAWS_ROOT = BENCHMARK_ROOT / "datasets" / "codeflaws"
CODEFLAWS_RAW_ROOT = CODEFLAWS_ROOT / "raw"
CODEFLAWS_PROCESSED_ROOT = CODEFLAWS_ROOT / "processed"
CODEFLAWS_METADATA_ROOT = CODEFLAWS_ROOT / "metadata"
CODEFLAWS_DOWNLOAD_ROOT = CODEFLAWS_ROOT / "downloads"
CODEFLAWS_REPORT_ROOT = BENCHMARK_ROOT / "reports"

CODEFLAWS_ARCHIVE_URL = (
    "https://www.comp.nus.edu.sg/~release/codeflaws/codeflaws.tar.gz"
)
CODEFLAWS_DEFECT_TABLE_URL = "https://codeflaws.github.io/defect-table.html"
CODEFLAWS_OFFICIAL_SITE = "https://codeflaws.github.io/"
CODEFLAWS_OFFICIAL_REPOSITORY = "https://github.com/codeflaws/codeflaws"

CODEFLAWS_ARCHIVE = CODEFLAWS_DOWNLOAD_ROOT / "codeflaws.tar.gz"
CODEFLAWS_MANIFEST = CODEFLAWS_METADATA_ROOT / "manifest.jsonl"
CODEFLAWS_PILOT = CODEFLAWS_METADATA_ROOT / "pilot.jsonl"
CODEFLAWS_EXCLUDED = CODEFLAWS_METADATA_ROOT / "excluded_cases.jsonl"
CODEFLAWS_CLASSIFICATION = CODEFLAWS_METADATA_ROOT / "defect_classes.json"
CODEFLAWS_DOWNLOAD_RECORD = CODEFLAWS_METADATA_ROOT / "download.json"
CODEFLAWS_VALIDATION_REPORT = CODEFLAWS_METADATA_ROOT / "validation_report.json"
CODEFLAWS_PILOT_RESULTS = CODEFLAWS_METADATA_ROOT / "pilot_results.jsonl"
CODEFLAWS_SAMPLE_ROOT = CODEFLAWS_ROOT / "sample"
CODEFLAWS_PILOT_REPORT = CODEFLAWS_REPORT_ROOT / "codeflaws_pilot_report.md"
CODEFLAWS_PILOT_REPORT_DATA = CODEFLAWS_REPORT_ROOT / "codeflaws_pilot_report.json"

BENCHMARK_METADATA_ROOT = BENCHMARK_ROOT / "metadata"
EXPERIMENT_ENVIRONMENT = BENCHMARK_METADATA_ROOT / "experiment_environment.json"
FAULT_LOCALIZATION_ROOT = BENCHMARK_ROOT / "results" / "fault_localization"
FAULT_LOCALIZATION_COVERAGE_ROOT = FAULT_LOCALIZATION_ROOT / "coverage"
FAULT_LOCALIZATION_RANKING_ROOT = FAULT_LOCALIZATION_ROOT / "rankings"
FAULT_LOCALIZATION_GROUND_TRUTH = (
    BENCHMARK_METADATA_ROOT / "fault_localization_ground_truth.jsonl"
)
FAULT_LOCALIZATION_EVALUATION = FAULT_LOCALIZATION_ROOT / "evaluation.json"
FAULT_LOCALIZATION_REPORT = (
    CODEFLAWS_REPORT_ROOT / "fault_localization_pilot_report.md"
)
FAULT_LOCALIZATION_BRANCH_EVALUATION = (
    FAULT_LOCALIZATION_ROOT / "branch_evaluation.json"
)
FAULT_LOCALIZATION_BRANCH_REPORT = (
    CODEFLAWS_REPORT_ROOT / "fault_localization_branch_report.md"
)

PILOT_RANDOM_SEED = 20260815
PILOT_TARGET_SIZE = 50
BENCHMARK_COMPILE_TIMEOUT_SECONDS = 20.0
BENCHMARK_RUN_TIMEOUT_SECONDS = 5.0
MANIFEST_SCHEMA_VERSION = "1.0.0"
